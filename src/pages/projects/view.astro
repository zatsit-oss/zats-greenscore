---
import Layout from '../../layouts/Layout.astro'
import RadarChart from '../../components/RadarChart.astro'
import { getAvailableEvaluationTypes } from '../../types/evaluation'

const evaluationTypes = getAvailableEvaluationTypes()
---

<Layout title="Project Details">
  <div class="max-w-4xl mx-auto hidden" id="projectContent">
    <div class="mb-8">
      <a href="/" class="link-back mb-4">
        &larr; Back to Dashboard
      </a>
      <div class="flex justify-between items-start flex-wrap gap-4">
        <div>
          <h1 class="text-4xl font-extrabold leading-tight mb-3" id="projectName">
            Project Name
          </h1>
          <!-- Evaluation Type Selector -->
          <div class="flex items-center gap-3">
            <label for="evaluationTypeSelect" class="text-sm font-medium text-muted">
              Evaluation:
            </label>
            <select id="evaluationTypeSelect" class="form-select">
              {evaluationTypes.map((evalType) => (
                <option value={evalType.id}>
                  {evalType.name}
                </option>
              ))}
            </select>
          </div>
        </div>
        <div class="flex gap-4 items-center">
          <span class="status-badge" id="projectStatus">Status</span>
          <button class="btn btn-secondary hidden" id="editBtn">Edit Evaluation</button>
          <button class="btn btn-danger hidden" id="deleteBtn" title="Delete Project">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
            </svg>
          </button>
        </div>
      </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <div class="md:col-span-2 space-y-8">
        <div class="glass-card">
          <h2 class="text-xl font-semibold mb-4">About this Project</h2>
          <p class="text-muted leading-relaxed" id="projectDescription">
            Description...
          </p>
        </div>

        <div class="glass-card">
          <h2 class="text-xl font-semibold mb-4">Timeline</h2>
          <div class="flex gap-8 mt-6">
            <div class="flex flex-col">
              <span class="text-xs text-muted uppercase tracking-wider mb-1">Created</span>
              <span class="font-medium" id="dateCreated">Oct 1, 2023</span>
            </div>
            <div class="flex flex-col">
              <span class="text-xs text-muted uppercase tracking-wider mb-1">Last Updated</span>
              <span class="font-medium" id="dateUpdated">Oct 1, 2023</span>
            </div>
          </div>
        </div>

        <!-- Score by Section Chart -->
        <div class="glass-card hidden" id="chartCard">
          <h2 class="text-xl font-semibold mb-4">Score by Section</h2>
          <RadarChart id="sectionRadarChart" />
        </div>
      </div>

      <div class="md:col-span-1">
        <div class="glass-card text-center">
          <h3 class="text-base font-medium text-muted mb-4" id="scoreTitle">Current Score</h3>
          <div class="flex justify-center mb-4">
            <div
              class="w-32 h-32 rounded-full border-8 flex items-center justify-center bg-white/5 shadow-[0_0_20px_rgba(0,0,0,0.1)]"
              id="scoreCircle"
            >
              <span class="text-5xl font-extrabold" id="projectScore">--</span>
            </div>
          </div>
          <p class="text-sm text-muted" id="scoreLabel">Level</p>
        </div>
      </div>
    </div>
  </div>

  <div id="loadingState" class="text-center py-20">
    <p class="text-[var(--color-text-muted)]">Loading project details...</p>
  </div>

  <div id="errorState" class="hidden text-center py-20">
    <h1 class="text-2xl font-bold text-[var(--color-text)] mb-4">
      Project Not Found
    </h1>
    <p class="text-[var(--color-text-muted)] mb-8">
      The project you are looking for does not exist or has been removed.
    </p>
    <a
      href="/"
      class="px-4 py-2 rounded-lg font-medium bg-[var(--color-primary)] text-white hover:bg-[var(--color-primary-hover)] transition-all"
    >
      Back to Dashboard
    </a>
  </div>

  <!-- Delete Confirmation Modal -->
  <div id="deleteModal" class="fixed inset-0 z-50 hidden items-center justify-center bg-black/50 backdrop-blur-sm">
    <div class="glass-card max-w-md w-full mx-4 p-6">
      <h2 class="text-xl font-bold mb-4">Delete Project?</h2>
      <p class="text-muted mb-2">
        Are you sure you want to delete
        <span class="font-semibold text-[var(--color-text)]" id="deleteProjectName">"Project Name"</span>?
      </p>
      <p class="text-sm text-red-400 mb-6">This action cannot be undone.</p>
      <div class="flex gap-4 justify-end">
        <button class="btn btn-secondary" id="cancelDeleteBtn">Cancel</button>
        <button class="btn btn-danger" id="confirmDeleteBtn">Delete</button>
      </div>
    </div>
  </div>
</Layout>

<script>
  import { getProject, saveProject, deleteProject } from '../../services/project-service'
  import { EvaluationType, EvaluationStatus, EVALUATION_TYPES, createEmptyEvaluation } from '../../types/evaluation'
  import type { Evaluation } from '../../types/evaluation'
  import type { Project } from '../../types/project'
  // API Green Score imports
  import { calculateScoresBySection, type Question } from '../../utils/apigreenscore-scoring'
  import surveyQuestions from '../../data/apigreenscore-survey-questions.json'
  // EROOM imports
  import { calculateEroomGlobalScore, getScoreInterpretation } from '../../utils/eroom-scoring'
  import type { EroomCategory, EroomAnswerValue } from '../../types/eroom'
  import eroomData from '../../data/eroom-data-model.json'

  const eroomCategories = eroomData.categories as EroomCategory[]

  // DOM Elements
  const evalTypeSelect = document.getElementById('evaluationTypeSelect') as HTMLSelectElement
  const statusEl = document.getElementById('projectStatus')
  const scoreTitleEl = document.getElementById('scoreTitle')
  const scoreEl = document.getElementById('projectScore')
  const scoreLabelEl = document.getElementById('scoreLabel')
  const scoreCircleEl = document.getElementById('scoreCircle')
  const chartCard = document.getElementById('chartCard')
  const deleteBtn = document.getElementById('deleteBtn')
  const deleteModal = document.getElementById('deleteModal')
  const deleteProjectNameEl = document.getElementById('deleteProjectName')
  const cancelDeleteBtn = document.getElementById('cancelDeleteBtn')
  const confirmDeleteBtn = document.getElementById('confirmDeleteBtn')

  // URL params
  const urlParams = new URLSearchParams(window.location.search)
  const projectId = urlParams.get('id')
  const requestedEvalType = urlParams.get('evaluationType') as EvaluationType | null

  // Helpers
  const formatDate = (dateString: string) => {
    if (!dateString) return 'N/A'
    return new Date(dateString).toLocaleDateString('en-US', {
      year: 'numeric',
      month: 'short',
      day: 'numeric'
    })
  }

  // Score interpretation for API Green Score (high = good)
  const getApiGreenScoreDetails = (score: number) => {
    if (score >= 90) return { text: 'Excellent', color: '#10b981', shadow: 'rgba(16, 185, 129, 0.2)' }
    if (score >= 75) return { text: 'Good', color: '#84cc16', shadow: 'rgba(132, 204, 22, 0.2)' }
    if (score >= 50) return { text: 'Needs Improvement', color: '#f59e0b', shadow: 'rgba(245, 158, 11, 0.2)' }
    return { text: 'Poor', color: '#ef4444', shadow: 'rgba(239, 68, 68, 0.2)' }
  }

  // Score interpretation for EROOM (high = more optimization potential = worse)
  const getEroomScoreDetails = (score: number) => {
    const interpretation = getScoreInterpretation(score)
    return {
      text: interpretation.level,
      color: interpretation.color,
      shadow: `${interpretation.color}33` // 20% opacity
    }
  }

  const getScoreDetails = (score: number, evalType: EvaluationType) => {
    if (evalType === EvaluationType.EROOM) {
      return getEroomScoreDetails(score)
    }
    return getApiGreenScoreDetails(score)
  }

  /**
   * Update the UI for a specific evaluation type
   */
  const updateEvaluationDisplay = (project: Project, evalType: EvaluationType) => {
    const evaluation = project.evaluations[evalType]

    // Score Title - adapt to evaluation type
    if (scoreTitleEl) {
      const evalMeta = EVALUATION_TYPES[evalType]
      scoreTitleEl.textContent = `Current ${evalMeta?.shortName || evalMeta?.name || 'Score'}`
    }

    // Status Badge
    if (statusEl) {
      if (evaluation) {
        const isCompleted = evaluation.status === EvaluationStatus.COMPLETED
        statusEl.textContent = isCompleted ? 'Completed' : 'In Progress'
        let statusClasses = 'text-sm px-3 py-1 rounded-full font-semibold uppercase tracking-wider border'
        statusClasses += isCompleted
          ? ' bg-emerald-500/10 text-[var(--color-status-completed)] border-emerald-500/20'
          : ' bg-amber-500/10 text-[var(--color-status-inprogress)] border-amber-500/20'
        statusEl.className = statusClasses
      } else {
        statusEl.textContent = 'Not Started'
        statusEl.className = 'text-sm px-3 py-1 rounded-full font-semibold uppercase tracking-wider border bg-slate-500/10 text-[var(--color-text-muted)] border-slate-500/20'
      }
    }

    // Score
    const score = evaluation?.score ?? null
    if (scoreEl && scoreLabelEl && scoreCircleEl) {
      if (score !== null) {
        scoreEl.textContent = score.toString()
        const details = getScoreDetails(score, evalType)
        scoreLabelEl.textContent = details.text
        scoreCircleEl.style.borderColor = details.color
        scoreCircleEl.style.boxShadow = `0 0 20px ${details.shadow}`
        scoreCircleEl.style.backgroundColor = details.shadow
      } else {
        scoreEl.textContent = '--'
        scoreLabelEl.textContent = evaluation ? 'Not Calculated' : 'Not Started'
        scoreCircleEl.style.borderColor = '#94a3b8'
        scoreCircleEl.style.boxShadow = 'none'
        scoreCircleEl.style.backgroundColor = 'transparent'
      }
    }

    // Radar Chart - Score by Section
    if (chartCard) {
      if (evaluation?.answers && Object.keys(evaluation.answers).length > 0) {
        let chartData: Array<{ label: string; score: number }> = []

        if (evalType === EvaluationType.EROOM) {
          // EROOM: Use category scores
          const { categoryScores } = calculateEroomGlobalScore(
            evaluation.answers as Record<string, EroomAnswerValue>,
            eroomCategories
          )
          chartData = categoryScores.map(cs => ({
            label: cs.categoryName,
            score: cs.score
          }))
        } else {
          // API Green Score: Use section scores
          const sectionScores = calculateScoresBySection(
            evaluation.answers,
            surveyQuestions as Question[]
          )
          chartData = sectionScores.map(s => ({ label: s.section, score: s.score }))
        }

        if (chartData.length > 0) {
          chartCard.classList.remove('hidden')
          requestAnimationFrame(() => {
            if (typeof (window as any).initRadarChart === 'function') {
              (window as any).initRadarChart('sectionRadarChart', chartData)
            }
          })
        } else {
          chartCard.classList.add('hidden')
        }
      } else {
        chartCard.classList.add('hidden')
      }
    }

    // Edit Button - update click handler
    // Re-query the button from DOM to get current element (after previous cloneNode operations)
    const currentEditBtn = document.getElementById('editBtn') as HTMLButtonElement | null
    if (currentEditBtn) {
      currentEditBtn.classList.remove('hidden')
      // Update button text based on evaluation state
      currentEditBtn.textContent = evaluation ? 'Edit Evaluation' : 'Start Evaluation'

      // Remove old listeners by cloning and replacing
      const newEditBtn = currentEditBtn.cloneNode(true) as HTMLButtonElement
      currentEditBtn.parentNode?.replaceChild(newEditBtn, currentEditBtn)

      newEditBtn.addEventListener('click', () => {
        if (!evaluation) {
          // Create new evaluation for this type only if it doesn't exist
          project.evaluations[evalType] = createEmptyEvaluation(evalType)
          saveProject(project)
        }
        // Don't reset existing data - audit page will load existing answers
        // Score will only be recalculated when user submits the audit
        const auditPage = evalType === EvaluationType.EROOM ? '/audit-eroom' : '/audit'
        window.location.href = `${auditPage}?projectId=${project.id}`
      })
    }

    // Update URL without reload
    const newUrl = new URL(window.location.href)
    newUrl.searchParams.set('evaluationType', evalType)
    window.history.replaceState({}, '', newUrl.toString())
  }

  // Main logic
  if (!projectId) {
    document.getElementById('loadingState')!.style.display = 'none'
    document.getElementById('errorState')!.classList.remove('hidden')
  } else {
    const project = getProject(projectId)

    if (project) {
      // Determine initial evaluation type
      const projectEvalTypes = Object.keys(project.evaluations) as EvaluationType[]
      const initialEvalType = requestedEvalType && project.evaluations[requestedEvalType]
        ? requestedEvalType
        : projectEvalTypes[0] || EvaluationType.API_GREEN_SCORE

      // Update static project info
      document.title = `${project.name} - Project Details`
      document.getElementById('projectName')!.textContent = project.name
      document.getElementById('projectDescription')!.textContent =
        project.description || 'No description provided.'
      document.getElementById('dateCreated')!.textContent = formatDate(project.createdAt)
      document.getElementById('dateUpdated')!.textContent = formatDate(project.updatedAt)

      // Set initial select value
      if (evalTypeSelect) {
        evalTypeSelect.value = initialEvalType

        // Listen for evaluation type changes
        evalTypeSelect.addEventListener('change', () => {
          const selectedType = evalTypeSelect.value as EvaluationType
          updateEvaluationDisplay(project, selectedType)
        })
      }

      // Initial display
      updateEvaluationDisplay(project, initialEvalType)

      // Delete button setup
      if (deleteBtn && deleteModal && deleteProjectNameEl && cancelDeleteBtn && confirmDeleteBtn) {
        deleteBtn.classList.remove('hidden')

        // Open modal
        deleteBtn.addEventListener('click', () => {
          deleteProjectNameEl.textContent = `"${project.name}"`
          deleteModal.classList.remove('hidden')
          deleteModal.classList.add('flex')
        })

        // Close modal
        const closeModal = () => {
          deleteModal.classList.add('hidden')
          deleteModal.classList.remove('flex')
        }

        cancelDeleteBtn.addEventListener('click', closeModal)

        // Close on backdrop click
        deleteModal.addEventListener('click', (e) => {
          if (e.target === deleteModal) closeModal()
        })

        // Confirm delete
        confirmDeleteBtn.addEventListener('click', () => {
          deleteProject(project.id)
          window.location.href = '/'
        })
      }

      // Show Content
      document.getElementById('loadingState')!.style.display = 'none'
      document.getElementById('projectContent')!.classList.remove('hidden')
    } else {
      document.getElementById('loadingState')!.style.display = 'none'
      document.getElementById('errorState')!.classList.remove('hidden')
    }
  }
</script>
